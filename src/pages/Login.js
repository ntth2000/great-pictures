import React from "react";
import AuthForm from "../components/Auth/AuthForm";

const Login = (props) => {
  return (
    <React.Fragment>
      <AuthForm />
    </React.Fragment>
  );
};
export default Login;
